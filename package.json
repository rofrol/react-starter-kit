{
  "name": "es6-module-test",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "devDependencies": {
    "babel-core": "^5.6.15",
    "babelify": "^6.1.2",
    "browserify": "^10.2.6",
    "hex-rgb": "^1.0.0",
    "live-server": "^0.7.1",
    "react": "^0.13.3",
    "uglifyjs": "^2.4.10",
    "watchify": "^3.2.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js | uglifyjs -mc > bundle.js",
    "watch": "watchify index.js --outfile bundle.js -dv",
    "live-server": "live-server"
  },
  "license": "MIT"
}
